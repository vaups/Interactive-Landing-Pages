$(document).ready(function() {
    // Put all your common code here
});

$(document).ready(function() {
    $.getJSON('config.json', function(config) {
        // Now you can use `config.logo`, `config.models`, etc.
    });
});